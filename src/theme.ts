import { createTheme, PaletteOptions } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#c24c5a',
    },
    secondary: {
      main: '#808080',
    },
  } as PaletteOptions
});